apps:
  - script   : ./dist/src/Server.js
    name     : 'sapsap-api'
    instances: 1
    exec_mode: cluster
    watch: false
    env:
      NODE_ENV: development
    env_production:
      NODE_ENV: production
